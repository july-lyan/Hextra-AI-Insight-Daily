# GitHub Pages 部署指南

本指南将帮助您将 Hextra AI Insight Daily 项目部署到 GitHub Pages。

## 📋 前置要求

1. 拥有一个 GitHub 账户
2. 已安装 Git
3. 已克隆项目到本地（已完成 ✅）

## 🚀 部署步骤

### 步骤 1: Fork 项目到您的 GitHub 账户

1. 访问项目地址：https://github.com/justlovemaki/Hextra-AI-Insight-Daily
2. 点击右上角的 **Fork** 按钮
3. 选择要 Fork 到的账户或组织
4. 等待 Fork 完成

### 步骤 2: 更新本地仓库的远程地址

如果您想将代码推送到您自己的 Fork 仓库，需要更新远程地址：

```bash
# 将 YOUR_USERNAME 替换为您的 GitHub 用户名
git remote set-url origin https://github.com/july-lyan/Hextra-AI-Insight-Daily.git

# 验证远程地址
git remote -v
```

### 步骤 3: 配置 GitHub Pages 设置

1. 进入您 Fork 的仓库页面
2. 点击 **Settings**（设置）标签页
3. 在左侧菜单中找到 **Pages** 选项
4. 在 **Source**（源）部分：
   - 选择 **GitHub Actions** 作为部署源
   - ⚠️ **重要**：不要选择 "Deploy from a branch"
5. 点击 **Save** 保存设置

### 步骤 4: 配置仓库变量（可选但推荐）

工作流使用了 `APP_BASE_URL` 变量来设置网站的基础 URL。您需要配置这个变量：

1. 在仓库的 **Settings** 页面
2. 找到左侧菜单的 **Secrets and variables** -> **Actions**
3. 点击 **Variables** 标签页
4. 点击 **New repository variable**
5. 添加以下变量：
   - **Name**: `APP_BASE_URL`
   - **Value**: `https://july-lyan.github.io` 或 `https://july-lyan.github.io/Hextra-AI-Insight-Daily`（如果使用子路径）
6. 点击 **Add variable** 保存

> **注意**：如果不设置此变量，工作流会使用默认的 GitHub Pages URL。

### 步骤 5: 触发 GitHub Actions 工作流

有几种方式可以触发部署：

#### 方式 1: 自动触发（推荐）
- 当您 Fork 项目后，工作流会在以下情况自动触发：
  - 推送到 `main` 分支
  - 定时任务（每 4 小时运行一次）

#### 方式 2: 手动触发
1. 进入仓库的 **Actions** 标签页
2. 在左侧选择 **Deploy Hugo site to Pages** 工作流
3. 点击 **Run workflow** 按钮
4. 选择分支（通常是 `main`）
5. 点击 **Run workflow** 开始部署

#### 方式 3: 推送代码触发
如果您修改了代码并想重新部署：

```bash
# 确保您在 main 分支
git checkout main

# 添加您的更改（如果有）
git add .

# 提交更改
git commit -m "Update content"

# 推送到您的仓库
git push origin main
```

推送后，GitHub Actions 会自动触发构建和部署。

### 步骤 6: 查看部署状态

1. 进入仓库的 **Actions** 标签页
2. 查看最新的工作流运行状态
3. 等待构建完成（通常需要 2-5 分钟）
4. 如果成功，您会看到绿色的 ✓ 标记

### 步骤 7: 访问您的网站

部署成功后，您的网站将在以下地址可用：

- **默认地址**：`https://july-lyan.github.io/Hextra-AI-Insight-Daily`
- **自定义域名**：如果您配置了自定义域名，则使用您的域名

> **提示**：首次部署可能需要几分钟时间才能生效。如果无法访问，请等待几分钟后重试。

## 🔧 常见问题

### Q1: 工作流运行失败怎么办？

**可能的原因：**
- 未设置 `APP_BASE_URL` 变量（通常不会导致失败，但可能影响 URL）
- Hugo 构建错误
- 权限问题

**解决方法：**
1. 检查 **Actions** 标签页中的错误日志
2. 确保 Pages 设置中选择了 **GitHub Actions** 作为源
3. 确保仓库有足够的权限（Settings -> Actions -> General -> Workflow permissions）

### Q2: 如何更新内容？

项目使用 Hugo 静态网站生成器。内容文件位于 `content/` 目录：
- `content/cn/` - 中文内容
- `content/en/` - 英文内容

修改内容后，提交并推送即可自动触发重新部署。

### Q3: 如何自定义域名？

1. 在仓库的 **Settings** -> **Pages** 中
2. 在 **Custom domain** 部分输入您的域名
3. 按照提示配置 DNS 记录
4. 等待 DNS 生效（可能需要几分钟到几小时）

### Q4: 工作流没有自动触发？

确保：
- 已正确设置 Pages 源为 **GitHub Actions**
- 代码已推送到 `main` 分支
- 仓库的 Actions 功能已启用（Settings -> Actions -> General）

## 📝 项目结构说明

```
.
├── .github/workflows/    # GitHub Actions 工作流配置
│   └── pages.yaml        # GitHub Pages 部署工作流
├── content/              # 网站内容
│   ├── cn/              # 中文内容
│   └── en/              # 英文内容
├── layouts/              # Hugo 模板文件
├── static/               # 静态资源（图片、CSS 等）
├── hugo.yaml            # Hugo 配置文件
└── README.md            # 项目说明
```

## 🎉 完成！

恭喜！您的 AI 日报网站应该已经成功部署到 GitHub Pages 了。

如果遇到任何问题，请：
1. 查看 GitHub Actions 的日志
2. 参考项目的 [Issues](https://github.com/justlovemaki/Hextra-AI-Insight-Daily/issues)
3. 查看 [Hugo 官方文档](https://gohugo.io/documentation/)

---

**最后更新**：2026-01-14
